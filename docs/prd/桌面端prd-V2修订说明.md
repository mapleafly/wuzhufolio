# 桌面端 PRD V2.0 修订说明与评审材料（回写 D27 / D28 / D29）

> **触发**：2026-09-13 人工拍板「回写《跨端共享规范》/PRD 正文」（P5 集成联调人工门，第三轮）。
> **依据**：决策档 [D27](../dev/decisions/D27-稳定币1比1折算口径.md)、[D28](../dev/decisions/D28-白名单收敛USDT锚定.md)、
> [D29](../dev/decisions/D29-负持仓不计入净值.md)；问题来源 `docs/test/integration-report.md` §8/§11。
> **版本动作**：`桌面端prd.md` **V1.9 → V2.0**（定稿）；`跨端共享规范.md` **V1.0 → V1.1**（定稿）。
> **评审门**：本节即评审输入——请人工核对下列差异清单；通过后 V2.0/V1.1 生效为新的需求基线。
> **评审结论（2026-09-13）**：✅ **通过**——人工在 P5 人工门给出「P5通过」，本修订说明随该门一并确认；
> `桌面端prd.md` **V2.0** 与《跨端共享规范》**V1.1** 即日起为新的需求基线（取代 V1.9 / V1.0）。
> **有效需求串**（`docs/dev/增量台账.md`）：PRD **V2.0** + Δ{D21, D24, D25, D26, D27, D28, D29}
> （D27/D28/D29 已并入正文，Δ 保留作为决策溯源与移动端对齐锚点）。

---

## 1. 差异清单（逐处：旧 → 新）

### 1.1 《跨端共享规范》V1.0 → V1.1

| 位置 | 旧 | 新 |
|------|----|----|
| 副标题/编号 | V1.0 | **V1.1**（新增文档版本历史表：V1.0 抽取基线 / V1.1 本次修订） |
| §1 名词「资产净值」 | 全部币种持仓按现价折算的总价值 | + **持仓数量为负的币种不计入** |
| §1 名词「可用现金余额」 | 现金类币种持仓**按当前价**折算的合计 | **USDT 恒按 1:1**；白名单内其他稳定币（扩展项）**按当前市价**；**负持仓币不计入** |
| §1 名词「稳定币白名单」 | 默认 USDT、USDC、DAI、TUSD，可在设置中扩展 | **默认仅 USDT（固定项，不可移除）**；其他稳定币可扩展/移除；**白名单决定"可用现金"统计范围，不决定折算方式** |
| §1 名词（新增） | — | **「1:1 锚定折算」**：基础法币为 USD 时 USDT 折算价恒为 1（优先于快照）；**锚定集合固定 = {USDT}，不可扩展** |
| §1 名词（新增） | — | **「持仓异常」**：只可能来自导入路径；其市值不计入净值与可用现金，界面标记 + 显式提示，补录/校准后恢复 |
| §2 折算价格解析 | 快照 → API 回填；离线"待定价" | 增加 **① USDT 1:1 锚定优先**；明确**当前估值与事件折算共用同一取数链**；USDT 24h 盈亏恒 0 |
| §2 导入路径例外 | 负持仓 → 标记「持仓异常」+ 引导 | + **负持仓币市值不计入净值与可用现金**，仪表盘显式提示被排除金额 |
| 引用关系 | 桌面端 V1.9 | 桌面端 **V2.0**；新增**移动端对齐提示**（本规范为唯一权威，移动端正文随下一版本对齐） |

### 1.2 桌面端 PRD V1.9 → V2.0

| 位置 | 旧 | 新 |
|------|----|----|
| 副标题 | V1.9 (MVP) 定稿版本 | **V2.0 (MVP) 定稿版本** |
| 文档版本历史 | 止于 V1.9 | 新增 **V2.0 行**（修订摘要见 §2） |
| 名词「可用现金余额」 | 按当前价折算 | USDT 1:1 + 扩展项市价 + 负持仓不计入 |
| 名词「稳定币白名单」 | 默认 USDT/USDC/DAI/TUSD | **默认仅 USDT（固定不可移除）**，其他可增删；白名单 ≠ 折算方式 |
| 名词（新增 2 条） | — | 「1:1 锚定折算」「持仓异常」（与共享规范逐字一致） |
| 全局说明「折算价格解析」 | 快照 → API 回填 | + USDT 1:1 优先；估值与折算同链；USDT 24h 恒 0 |
| 全局说明「导入路径例外」 | 标记「持仓异常」 | + 负持仓市值不计入净值/可用现金 + 显式提示 |
| 故事 6.1（设置项「稳定币白名单」） | 查看并扩展（默认 4 币） | 默认仅 USDT 固定；其他可扩展与移除；USDT 1:1、扩展项市价 |
| 故事 7.2-2（仪表盘总资产净值） | Σ 全部币种持仓市值 | + **持仓为负的币种不计入** |
| 附录 A 用例 6（负持仓补增资） | 标记异常 → 补增资消除 | + **异常期间市值不计入净值/可用现金**，恢复后自动重新计入 |

**未改动**（明确保持）：法币角色与归一化规则（法币计价对 1:1 映射稳定币）、校准差额账务与锚点语义、
重放校验规则（手动路径阻止负持仓）、备份/加密/行情/时限等全部其余条款。

---

## 2. 影响面核对表（回写是否闭环）

| 下游 | 状态 |
|------|------|
| 代码（引擎/数据/UI） | ✅ 已实现并回归（D27 → D28 收敛；D29 聚合口径 + 仪表盘提示） |
| 测试 | ✅ 663 用例（656 执行 0 失败 + 7 跳过）+ detekt 0 + 编译警告 0 |
| `docs/tech/data-model.md` §3 | ✅ 派生口径注（折算 + 负持仓） |
| `docs/tech/api-contracts.md` §3 | ✅ 引擎契约补注（D27+D28 / D29） |
| `docs/tech/task-breakdown.md` | ✅ T4.6（含 D28 修订注）+ T4.7 |
| `docs/design/interaction.md` | ✅ §2.8「持仓异常币的净值口径」 |
| `docs/design/prototype/*` | ✅ 行情页自选种子收敛为 USDT（`.html` + `prototype-verify.js` 断言复跑 errors=[]） |
| `docs/dev/decisions/*` | ✅ D27（标注被 D28 修订）/ D28 / D29 |
| `docs/dev/增量台账.md` | ✅ D28/D29 行（amend 追加，不覆盖 D27）+ 有效需求串 |
| `docs/test/integration-report.md` | ✅ §8 裁决记录 + §11-4/§11-9 实施证据 |
| **移动端 PRD/SRD** | ⏳ **随下一版本对齐**（本轮不启用移动端工作，见 `AGENTS.md §1`；共享规范为唯一权威，已在 V1.1 头部与本表登记） |

---

## 3. 评审要点（请人工确认）

1. **口径是否符合预期**：USDT 唯一锚定（票面 1:1）；白名单默认仅 USDT、其他稳定币"可增删 + 按市价折算"；
   负持仓币市值不计入净值与可用现金且界面显式提示。
2. **版本动作是否认可**：PRD V2.0 / 共享规范 V1.1（替代 V1.9/V1.0 成为新基线）。
3. **移动端对齐方式是否认可**：本轮只改共享规范 + 桌面端正文；移动端 PRD/SRD 待下一版本启动时按共享规范对齐
   （不阻塞桌面端 P5/P6/P7）。
4. **黄金用例 6 的补充是否认可**（负持仓期间市值不计入，恢复后自动计入）。
